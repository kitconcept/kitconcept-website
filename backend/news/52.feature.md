Added multilingual support to the distribution: a new `available_languages` answer drives `plone.available_languages` and `plone.default_language`, and `plone.app.multilingual` is installed automatically when more than one language is selected. @ericof
